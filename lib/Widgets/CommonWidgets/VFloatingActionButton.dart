import 'package:flutter/material.dart';

class VFloatingActionButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      child: Icon(Icons.create),
      onPressed: () {},
    );
  }
}
